'use strict'

var gMemes = []
var gCurrMeme = {
    selectedImgId: 1,
    selectedLineIdx: 0,
    lines: [
        {
            txt: '',
            fontSize: 20,
            align: 'left',
            color: 'black',
            fontFamily: 'Montserrat',
            //x: 10,
            y: 20
        }
    ]
}


function getMeme(memeId) {
   // const selectedMeme = gImgs.find(img => img.id === memeId)
    //gMeme = 
}
